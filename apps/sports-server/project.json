{
  "name": "sports-server",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/sports-server/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/sports-server",
        "main": "apps/sports-server/src/main.ts",
        "tsConfig": "apps/sports-server/tsconfig.app.json",
        "assets": ["apps/sports-server/src/assets"],
        "isolatedConfig": true,
        "webpackConfig": "apps/sports-server/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "sports-server:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "sports-server:build:development"
        },
        "production": {
          "buildTarget": "sports-server:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/sports-server/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/sports-server/jest.config.ts"
      }
    }
  },
  "tags": []
}
